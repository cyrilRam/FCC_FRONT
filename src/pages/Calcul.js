import React from 'react';
import Navigation from "../components/Navigation";

const Calcul = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Calcul;